import {Player} from '@/types/game';import {classes,races} from '@/data/game';import {Modal} from './ui';
export function CharacterSheet({player,onClose}:{player:Player;onClose:()=>void}){const c=classes.find(x=>x.id===player.classId),r=races.find(x=>x.id===player.raceId);const statNames:{[K in keyof Player['stats']]:string}={strength:'כוח',dexterity:'זריזות',constitution:'חוסן',intelligence:'תבונה',wisdom:'חכמה',charisma:'כריזמה'};return <Modal title="דף הדמות" onClose={onClose}><div className="sheet-head"><span>{player.portrait}</span><div><h3>{player.name}</h3><p>{c?.name} • {r?.name} • דרגה {player.level}</p></div></div><div className="stats-grid">{Object.entries(player.stats).map(([key,value])=><div key={key}><small>{statNames[key as keyof Player['stats']]}</small><strong>{value}</strong></div>)}</div><div className="summary-grid"><span>חיים <b>{player.health}/{player.maxHealth}</b></span><span>שריון <b>{player.armor}</b></span><span>ניסיון <b>{player.experience}</b></span><span>זהב <b>{player.gold}</b></span><span>מוניטין <b>{player.reputation}</b></span></div><h3 className="section-title">ציוד</h3><div className="equipment-grid">{['נשק','שריון','קסדה','מגפיים','טבעת','קמע'].map((slot,i)=><div key={slot}><span>{i===0?'⚔️':'◇'}</span><small>{slot}</small></div>)}</div></Modal>}
