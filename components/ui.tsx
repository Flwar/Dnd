'use client';
import {ReactNode} from 'react';import {motion} from 'framer-motion';import {X} from 'lucide-react';
export function FantasyButton({children,onClick,disabled=false,className='' }:{children:ReactNode;onClick?:()=>void;disabled?:boolean;className?:string}){return <motion.button whileHover={disabled?{}:{scale:1.02}} whileTap={disabled?{}:{scale:.97}} disabled={disabled} onClick={onClick} className={`gold-button ${className}`}>{children}</motion.button>}
export function Panel({children,className='' }:{children:ReactNode;className?:string}){return <section className={`stone-panel ${className}`}>{children}</section>}
export function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:ReactNode}){return <div className="modal-backdrop" role="dialog" aria-modal="true"><motion.div initial={{opacity:0,scale:.94}} animate={{opacity:1,scale:1}} className="modal-card"><div className="flex items-center justify-between border-b border-gold/30 pb-3"><h2 className="title-font text-2xl text-gold">{title}</h2><button onClick={onClose} aria-label="סגירה" className="icon-button"><X/></button></div><div className="mt-4 overflow-y-auto">{children}</div></motion.div></div>}
