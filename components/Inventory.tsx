'use client';import {useState} from 'react';import {Item} from '@/types/game';import {Modal} from './ui';
export function Inventory({items,onClose}:{items:Item[];onClose:()=>void}){const[selected,setSelected]=useState(items[0]);return <Modal title="תיק הציוד" onClose={onClose}><div className="inventory-layout"><div className="inventory-grid">{items.map(item=><button onClick={()=>setSelected(item)} className={`inventory-item ${selected.id===item.id?'selected':''}`} key={item.id}><span>{item.icon}</span><b>{item.quantity}</b><small>{item.name}</small></button>)}{Array.from({length:7}).map((_,i)=><div className="empty-slot" key={i}/>)}</div><aside className="item-details"><div className="text-6xl">{selected.icon}</div><h3>{selected.name}</h3><span className={`rarity rarity-${selected.rarity.replace(' ','-')}`}>{selected.rarity}</span><p>{selected.description}</p><dl><div><dt>סוג</dt><dd>{selected.type}</dd></div><div><dt>כמות</dt><dd>{selected.quantity}</dd></div></dl></aside></div></Modal>}
