'use client';import {Dialogue} from '@/types/game';import {motion} from 'framer-motion';
export function DialoguePanel({dialogue,onChoice}:{dialogue:Dialogue;onChoice:(id:string)=>void}){return <div className="dialogue"><div className="speaker"><span>{dialogue.portrait}</span><div><small>דובר</small><b>{dialogue.speaker}</b></div></div><p>״{dialogue.text}״</p><div className="grid gap-2">{dialogue.choices.map((choice,i)=><motion.button whileHover={{x:-4}} onClick={()=>onChoice(choice.id)} className="choice-button" key={choice.id}><span>{i+1}</span>{choice.text}</motion.button>)}</div></div>}
