import {Player} from '@/types/game';import {classes} from '@/data/game';
export function PlayerStatus({player}:{player:Player}){const resource=classes.find(c=>c.id===player.classId)?.resource;return <header className="status-bar"><div className="text-3xl">{player.portrait}</div><div><strong className="text-gold">{player.name}</strong><p className="text-xs text-stone-400">דרגה {player.level}</p></div><Bar label="חיים" value={player.health} max={player.maxHealth} color="bg-red-700"/><Bar label={resource||'אנרגיה'} value={player.energy} max={player.maxEnergy} color="bg-blue-700"/><Bar label="ניסיון" value={player.experience} max={100} color="bg-amber-600"/></header>}
function Bar({label,value,max,color}:{label:string;value:number;max:number;color:string}){return <div className="min-w-28 flex-1"><div className="mb-1 flex justify-between text-xs"><span>{label}</span><span>{value}/{max}</span></div><div className="h-2 overflow-hidden rounded bg-black/60"><div className={`h-full ${color}`} style={{width:`${Math.max(0,value/max*100)}%`}}/></div></div>}
