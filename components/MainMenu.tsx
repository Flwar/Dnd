'use client';
import {BookOpen,Cog,Info,Play,Users} from 'lucide-react';import {motion} from 'framer-motion';import {FantasyButton} from './ui';import {texts} from '@/data/texts';
export function MainMenu({hasSave,onNew,onContinue,onSettings,onNotice}:{hasSave:boolean;onNew:()=>void;onContinue:()=>void;onSettings:()=>void;onNotice:(s:string)=>void}){return <main className="menu-screen"><div className="crown-mark">♛</div><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="text-center"><p className="mb-3 tracking-[.35em] text-gold/70">אגדת שבעת הרסיסים</p><h1 className="title-font text-5xl font-bold text-gold md:text-7xl">{texts.title}</h1><p className="mx-auto mt-5 max-w-xl text-lg text-stone-300">{texts.subtitle}</p></motion.div><div className="mt-10 grid w-full max-w-sm gap-3"><FantasyButton onClick={onNew}><Play/> {texts.newJourney}</FantasyButton><FantasyButton disabled={!hasSave} onClick={onContinue}><BookOpen/> {texts.continueJourney}</FantasyButton><FantasyButton onClick={()=>onNotice('המשחק המקוון ייפתח בפרק הבא. ההרפתקה היחידנית זמינה כבר עכשיו.')}><Users/> {texts.online}</FantasyButton><div className="grid grid-cols-2 gap-3"><FantasyButton onClick={onSettings}><Cog/> {texts.settings}</FantasyButton><FantasyButton onClick={()=>onNotice('משחק תפקידים מקורי המתרחש בעולם אֵידרה. נבנה באהבה להרפתקאות משותפות.')}><Info/> {texts.about}</FantasyButton></div></div>{!hasSave&&<p className="mt-4 text-sm text-stone-500">״המשך המסע״ ייפתח לאחר יצירת דמות</p>}</main>}
